package com.grudus.minutnikpoprzezwieki.utils

import java.util.concurrent.TimeUnit

const val TIME_FORMAT = "mm:ss"
val oneSecond = TimeUnit.SECONDS.toMillis(1)


const val TIME_PREFERENCE = "TIME"
const val DEFAULT_TIMER_MINUTES = 5L